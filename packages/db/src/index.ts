/**
 * packages/db — esquema, migraciones y tipos generados de Supabase.
 *
 * Las migraciones SQL viven en `migrations/` (aplicadas al proyecto Supabase
 * `tecni` vía MCP, versionadas aquí para reproducibilidad). Los tipos
 * generados con `supabase gen types` llegan cuando el esquema de esta fase
 * esté completo — ver docs/01-ARCHITECTURE.md sección 3 y
 * docs/05-RLS-SECURITY.md.
 */
export const DB_PACKAGE_NAME = "@tecni/db";
